# Assessment

### 1.

Which command should you run first when users report that a service is unreachable?

A. tcpdump

B. iptables -L

C. ss -tlnp

D. ping

---

### 2.

What does the LISTEN state indicate?

A. A firewall rule is active.

B. The service is waiting for incoming connections.

C. A client is actively transferring data.

D. DNS resolution succeeded.

---

### 3.

What is the primary purpose of `tcpdump`?

A. Restart services.

B. Capture and inspect network packets.

C. Configure routing.

D. Resolve DNS names.

---

### 4.

What does the `-n` flag in `tcpdump` do?

A. Captures only new packets.

B. Disables DNS name resolution.

C. Displays node information.

D. Uses network mode.

---

### 5.

What is a `.pcap` file?

A. A firewall configuration.

B. A saved packet capture.

C. A routing table.

D. A socket list.

---

### 6.

How are `iptables` rules evaluated?

A. Bottom to top.

B. Randomly.

C. First matching rule wins.

D. Last matching rule wins.

---

### 7.

Why should packet capture begin before reproducing the issue?

A. To reduce CPU usage.

B. To ensure the traffic is recorded.

C. To restart the network.

D. To clear the firewall.

---

### 8.

A service is listening, packets are arriving, but clients still cannot connect. What should you investigate next?

A. Disk space

B. DNS cache

C. Firewall policy

D. Hostname
